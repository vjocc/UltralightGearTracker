Intentional addition — a plain data table for order/project lists, styled with the same border and overline-label conventions as the rest of the system.

```jsx
<Table columns={['Név','Videó','Állapot']} rows={[['Kata B.','Esküvő 2024','Kész']]} />
```
