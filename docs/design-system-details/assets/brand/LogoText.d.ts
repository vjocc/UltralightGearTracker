import * as React from 'react';
export interface LogoTextProps {
  className?: string;
  style?: React.CSSProperties;
  color?: boolean;
}
export declare const LogoText: React.FC<LogoTextProps>;
export default LogoText;
